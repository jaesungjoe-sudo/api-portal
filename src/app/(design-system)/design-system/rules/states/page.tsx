import { ComingSoon } from "@/components/design-system/ComingSoon";

export default function StatesPage() {
  return (
    <ComingSoon
      title="States"
      description="Loading · Empty · Error · Disabled — surface × state matrix + baselines."
      doc="design-system/rules/states.md"
    />
  );
}
