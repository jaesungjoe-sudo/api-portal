import { ComingSoon } from "@/components/design-system/ComingSoon";

export default function TokensPage() {
  return (
    <ComingSoon
      title="Tokens"
      description="Color, typography, spacing, radius, shadow tokens — visual gallery."
      doc="design-system/tokens/colors.json"
    />
  );
}
